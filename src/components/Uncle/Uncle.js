import React from 'react';

const Uncle = ({ house }) => {
    return (
        <div>
            <h1>Uncle </h1>
            <small>House: {house}</small>
        </div>
    );
};

export default Uncle;