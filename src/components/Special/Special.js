import React from 'react';

const Special = ({ ring }) => {
    return (
        <div>
            <h1>Special</h1>
            <small>Gift: {ring}</small>
        </div>
    );
};

export default Special;